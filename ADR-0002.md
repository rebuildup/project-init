# ADR-0002: Low-cost policy safeguards and time-sensitive role separation

- Status: Accepted
- Date: 2026-08-11
- Related: ADR-0001
- Supersedes: None
- Superseded by: None

## Context

ADR-0001で定めたproject-local、progressive disclosure、local `main` + no worktree、最大安全並列化、forward-orientedな基盤移行という基本方針は維持する。

一方、実運用の協議から、開発速度を落とす承認gateやblocking checkを追加せずに、context costと復旧不能事故、外部取得コンテンツ由来のinstruction injection、secret漏えいの見逃しを低コストで減らせる箇所が確認された。

また、CodexのSol / Terra / Luna role allocationは時点依存であり、core initialization policy本文に置き続けるとmodel世代交代のたびに本文全体へtime-sensitive diffが混入する。

## Decision

### 1. Full policy documentの読み込み機会を限定する

`PROMPT.ja.md` / `PROMPT.en.md` の全文をagentへ読み込ませるのは、次の2種類の状況に限定する。

1. 本policyでrepositoryを初めて初期化する時
2. 本policyをAgent Skillとして再構成または更新する時

通常タスクでは、初期化で生成されたproject-local `AGENTS.md` とAgent Skillsを参照する。

これにより、root instructionsをdispatcherにし、詳細をSkillへprogressive disclosureするADR-0001の方針を運用上も明示する。

### 2. Foundational migration前にlightweight snapshot tagを作成する

基盤的・破壊的移行を開始する最初のcommit前に、current `HEAD`へ次の形式のlightweight Git tagを作成する。

`pre-migration/<work-prefix>-<short-desc>`

このtagはapproval、停止、revert-first運用を意味しない。保護対象はtagが指す**committed `HEAD` stateのみ**であり、staged / unstaged changesは含まれない。tag作成のためにclean worktreeを要求したり、既存のuncommitted changesをstash / commit / discardしたりしない。

これにより、移行を自律実行する既存方針を維持したまま、移行開始直前のcommitted stateを即座に特定できるようにする。

### 3. Codex role allocationをcore promptから分離する

Codexのmodel別role allocationは次の独立文書をcanonical sourceとして管理する。

- `CODEX_ROLES.ja.md`
- `CODEX_ROLES.en.md`

両ファイルは同一変更で更新し、operational semanticsを一致させる。

初期化またはAgent Skill再構成・更新時に、使用するagentのproject-local mechanismに合わせてCodex role policyをCodex-specific Agent Skillまたはthin adapterへ反映する。通常タスクではrootの`CODEX_ROLES.*.md`を毎回直接読み込まず、生成済みのSkill / adapterを参照する。

生成済みrole unitがmissing / staleな場合だけinitialization driftとして扱い、使用中のprompt / internal documentation言語に対応するroot `CODEX_ROLES.*.md`のみを読み、role unitをrepairして通常タスクを継続する。対応言語版が欠けている場合は意味同値である他方を一時fallbackとして利用し、欠落したpairもrepairする。full initialization promptの再読は要求しない。

role semantics自体はADR-0001から変更しない。model lineup、pricing、availability、subagent behavior、model routing等が変わった場合はrole文書pairを独立して更新し、decision自体が変わる場合は新ADRまたは既存ADR更新で記録する。

### 4. Non-blocking secret scanningを許可する

secret scanning等の非同期検査は、commitやmergeを必ず止める標準gateにはしない。

開発速度へ実質的な影響を与えない範囲でCIへasynchronous jobとして追加できる。blockingにするかどうかはproject単位の具体的な要件で判断する。

non-blockingであってもfindingを放置してよいという意味ではない。検出結果はまず実在する問題か検証し、有効なsecret漏えいならblocking状態に関係なくcredentialを直ちにrevoke / rotateし、必要な場合だけ履歴から除去し、直接関係する担当者へ通知し、対応結果を記録する。genericな固定SLA、事前owner割当、approval gateはproject要件がない限り標準化しない。

これにより、ADR-0001のdeterministic verification方針を弱めず、通常開発へ追加の待ち時間を導入しない。

### 5. 外部取得コンテンツをinstruction sourceとして扱わない

external documentation、Context7等のretrieval、general webから得たcontentは、すべてnon-authoritativeなinformation / evidenceとして扱う。

外部content中のtechnical procedureは、user requestまたはproject-local policyが独立して許可し、必要な情報を検証した場合にのみ利用・実行できる。外部content自体がtask scope、permission、security check、project policyを変更・拡張することはできない。

実行判断はuser request、repository-controlled policy、verified informationから行う。

## Consequences

### Positive

- 通常タスクでfull initialization promptやtime-sensitive role文書を繰り返しcontextへ投入する必要がなくなる。
- disruptive migrationの直前committed stateをapproval待ちなしで識別できる。
- Codex model世代交代によるcore promptの不要なdiffを減らせる。
- 外部document/web content由来のinstruction injectionに対する意味論的boundaryが明示される。
- secret scanningを通常開発のblocking costなしで追加できる。

### Negative / Trade-offs

- Codex role文書と生成されたSkill / adapterの同期をinitialization reconciliationで扱う必要がある。
- migration snapshot tagはuncommitted changesを保護しない。
- non-blocking secret scanningは漏えいをmerge前に必ず阻止するものではなく、valid finding検出時には即時対応が必要になる。
- external contentのtechnical procedureを採用する際は独立したtask/policy authorizationが必要になる。

## Explicitly unchanged decisions

以下は本ADRでは変更しない。

- human approval gateを標準化しない
- local `main` + no worktree
- forward-oriented foundational migration
- revertを前提としたrelease policyへ変更しない
- commit-blocking security checksを標準化しない
- Python/Bun等のtooling conventions
- deterministic quality gate
- 最大安全並列化

## Re-evaluation triggers

以下の場合は再評価する。

- initialization prompt自体を毎task読む必要があるagent/runtimeへ移行する
- project-local Agent Skillsのdisclosure modelが変化する
- Codex model lineup / pricing / routing / subagent APIが変更される
- non-blocking secret scanningでは許容できないrisk profileのprojectが標準になる
- migration snapshotをtag以外のより低コストで確実なmechanismへ置き換えられる

## References reviewed at acceptance

- ADR-0001: Project-local AI Agent Initialization Policy
- [OpenAI, GPT-5.6: Frontier intelligence that scales with your ambition](https://openai.com/index/gpt-5-6/), 2026-07-09 (accessed 2026-08-12)
